# Todo

- 自动保存 做成开关
- 图片右键菜单支持居中 左对齐 右对齐  还可以ai想想增加什么
- 单击 = 预览阅读，不污染标签栏。双击 / 修改 / 手动固定 = 正式打开。这和你的“轻量化 Markdown 阅读编辑器”定位很搭，因为用户浏览很多 md 文件时，不会一会儿开出一堆标签页。

# Bug


- 资源管理器 使用图标创建文件夹的时候 回车 整个文件夹结构会刷新一下 并且跳到最上方
- 加入一个机制更新目录  现在手动在外面删除文件文件夹 资源管理器没有改变
- 两个列表中间 输入好多段落 保存之后就没有了  怀疑不只是两个段落之间存在

# 考虑实现

**问题 7：段落菜单里的列表区怎么取舍？**

我的推荐：**保留有序列表、无序列表、任务列表；暂时不放任务状态和列表缩进**。\
这样更接近 Typedown 的轻量结构。任务状态 >、列表缩进 > 属于进阶操作，一级菜单会变长，而且很多时候可以通过快捷键或编辑器行为处理。

你选：

1. **只保留有序列表、无序列表、任务列表**
2. **加上任务状态 >**
3. **加上列表缩进 >**
4. **任务状态和列表缩进都加上**

