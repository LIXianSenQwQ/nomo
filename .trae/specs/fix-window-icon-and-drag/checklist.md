- [x] 任务栏图标不再是红色的报错图标（`src-tauri/icons/icon.ico` 和 `icon.png` 是有效的图像文件）。
- [x] `tauri.conf.json` 中配置的图标路径与实际生成的图标文件匹配。
- [x] 在桌面端运行项目时，将鼠标悬停在窗口最顶部边缘（约 4px），能够出现系统的垂直缩放指针，且可以正常缩放窗口。
- [x] 在标题栏空白处拖动，可以正常移动窗口，且菜单按钮和右侧窗口控制按钮可以正常点击。
